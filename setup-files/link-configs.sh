#!/bin/env bash

ln ~/.config/.gitconfig ~/.gitconfig -s

ln ~/.config/tmux/.tmux.conf ~/.tmux.conf -s

ln ~/.config/.zshrc ~/.zshrc -s

ln ~/.config/nvim/.vimrc ~/.vimrc -s

ln ~/.config/.ssh ~/.ssh -s
